spring:
  data:
    mongodb:
      host: localhost
      database: library
      port: 0

  shell:
    interactive:
      enabled: false

mongock:
  runner-type: "ApplicationRunner"
  change-logs-scan-package:
    - ru.otus.dlyubanevich.mongock.changelog

logging:
  level:
    root: ERROR